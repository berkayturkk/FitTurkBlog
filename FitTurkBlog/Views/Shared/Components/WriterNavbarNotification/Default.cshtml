@using FitTurkBlog.Entities.Concrete
@model List<Notification>
@{
    Layout = null;
}

<li class="nav-item">
    <a class="nav-link" href="/Notification/AllNotification/">
        <span class="menu-title">
            Bildirimler
            @if (Model.Count > 0)
            {
                <span style="color:red; font-weight:bolder; margin-left:52.95px">(@Model.Count)</span>
            }
            else
            {
                <span style="color:red; font-weight:bolder; margin-left:52.95px">(0)</span>
            }
        </span>
        <i class="mdi mdi-bell-ring-outline menu-icon"></i>
    </a>
</li>

